export class Todo {
    constructor(title, description, dueDate, priority, groupLabel) {
        this.title = title;
        this.description = description;
        this.dueDate = dueDate;
        this.priority = priority;
        this.groupLabel = groupLabel;
    }
}